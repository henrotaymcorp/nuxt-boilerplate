#!/bin/bash
cd ../

docker run \
  -it \
  --rm \
  --user node:node \
  -v "$PWD":/usr/src/app \
  -w /usr/src/app node:16-alpine \
  yarn install && \

cp .env.docker .env
docker-compose build --no-cache

cd scripts
